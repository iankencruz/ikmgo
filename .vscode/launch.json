{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch Package",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/api/${fileBasename}",
      "trace": "verbose" // Add this line for detailed logs
    }
  ]
}
